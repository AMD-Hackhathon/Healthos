# Healthos

## OCR setup

Scanned PDF/image report extraction uses `pytesseract`, `Pillow`, and `pdf2image`.
Install these system tools and make sure they are available on `PATH`:

- Tesseract OCR (`tesseract`)
- Poppler (`pdftoppm`) for scanned PDF page conversion

start the job
